<div class="neon-bg d-flex justify-content-center align-items-center vh-100">
  <div class="login-card text-center p-5">
    <h2 class="logo-title mb-3">💬 <span>ChatLifeApp</span></h2>
    <p class="subtitle mb-4">Conéctate al futuro de la conversación</p>

    <%= form_for(:session, html: { class: "futuristic-form" }) do |f| %>
      <div class="form-group text-left">
        <%= f.label :username, 'Ingrese su username', class: 'form-label text-glow' %>
        <%= f.text_field :username, class: 'form-control futuristic-input', autocomplete: 'off', placeholder: 'Tu nombre de usuario' %>
      </div>
      <%= f.submit 'Iniciar sesión', class: 'btn btn-futuristic w-100 mt-3' %>
    <% end %>
  </div>
</div>
